#include <stdio.h>
int main(void){
  int a, b, c, min;
  
  printf("aを入力");
  scanf("%d", &a);
  printf("bを入力");
  scanf("%d", &b);
  printf("cを入力");
  scanf("%d", &c);
  ①;
  if( min ② b ){
    min = b;
  }
  if( min > c ){
    ③;
  }
  printf("最小の値は%d\n", min);

  return 0;
}