#include <stdio.h>
int main(void){
  float d, r, h, v;
  float pai = 3.14;

  printf("直径を入力：");
  scanf("%f", &d );
  printf("高さを入力：");
  scanf("%f", &h );
  r = ① / 2.0;
  v = pai * ② * h;
  ③("体積は%f\n", v );
  
  return 0;
}