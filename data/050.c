#include <stdio.h>
int main(void){
  int a, b, temp;
  
  printf("aを入力");
  scanf("%d", &a );
  printf("bを入力");
  scanf("%d", &b );
  if( a ① b ){
    temp = ②;
    a = b;
    b = ③;
  }
  printf("a = %d  b = %d\n", a, b );

  return 0;
}