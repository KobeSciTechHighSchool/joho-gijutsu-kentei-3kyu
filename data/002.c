#include <stdio.h>
int main(void){
  float a, b, h, s;

  printf("上底aを入力" );
  scanf("%f", &a);
  printf("下底bを入力" );
  scanf("%f", &b);
  printf("高さhを入力" );
  scanf("%f", &h);
　s = ①;
  ②("面積は%f\n", ③);

  return 0;
}