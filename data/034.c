#include <stdio.h>
#include <math.h>
int main(void){
  float a, b, c, k;
  
  printf("a, b,の辺の長さを入力");
  scanf("%f %f", ① );
  c = sqrt( ② );
  k = a + b + c;
  ③("斜辺の長さは%f\n", c );
  ③("三角形の周囲の長さは%f\n", k );
  
  return 0;
}