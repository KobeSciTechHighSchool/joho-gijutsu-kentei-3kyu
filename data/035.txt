#include <stdio.h>
int main(void){
  int k, p, total;
  float avg;

  total = 0;
  for( k = 1; k <= 10; k++ ){
    printf("商品の価格入力");
    scanf("①", &p);
    total = total + ②;
  }
  ③ = total / 10.0;
  printf("合計=%d\n", total );
  printf("平均=%f\n", avg );
  
  return 0;
}