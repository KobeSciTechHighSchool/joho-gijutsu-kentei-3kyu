#include <stdio.h>
int main(void){
  float r, d, a, s, pai = 3.14;
  
  printf("半径を入力してください：" );
  scanf("%f", ①);
  printf("中心角を入力してください：" );
  scanf("%f", &d);
  ② = r * r * pai;
  s = a * d / 360.0;
  printf("面積は%f\n", ③);

  return 0;
}