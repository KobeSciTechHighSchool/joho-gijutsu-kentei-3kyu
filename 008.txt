#include <stdio.h>
int main(void){
  int k, m, total;

  total = ‡@;
  ‡A( k=1; k<40; k++ ){
    printf("®”‚ð“ü—Í=>");
    scanf("%d", &m );
    ‡B( m > 0 ){
      total = total + m;
    }
  }
  printf("‡Œv=>%d\n", total );

  return 0;
}