#include <stdio.h>
int main(void){
  int d, c, m;
  
  c = 0;
  for( m = 1; m <= 10; m++ ){
    scanf("%d", &d);
    if( d ① 100){
      printf("OK\n");
      c = ②;
    }
  }
  printf("100以上のデータ個数%d\n", ③ );
  
  return 0;
}