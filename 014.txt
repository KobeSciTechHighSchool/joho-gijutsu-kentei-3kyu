#include <stdio.h>
int main(void){
  int kazu, wa;

  wa = 0;
  kazu = 4;
  ①( kazu ② 99 ){
      wa = wa + kazu;
      printf("４の倍数=%d\n", kazu);
      ③ = kazu + 4;
  }
  printf("合計=%d\n", wa );

  return 0;
}