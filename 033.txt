#include <stdio.h>
int main(void){
  float a, b, h, s;
  
  printf("上底aを入力");
  ①("%f", &a );
  printf("下底bを入力");
  ①("%f", &b );
  printf("高さhを入力");
  ①("%f", &h );
  s = ②;
  printf("面積は%f\n", ③);
  
  return 0;
}