#include <stdio.h>
int main(void){

  float d, r, h, v;
  float pai = 3.14;
  
  printf("直径を入力");
  scanf("%f", &d );
  printf("高さを入力");
  scanf("%f", ① );
  r = d / 2.0;
  v = pai * r * r * h;
  ②("体積は%f\n", ③);
  
  return 0;
}