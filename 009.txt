#include <stdio.h>
#include <math.h>
int main(void){
  float x, y, z, k, s;

  printf("x‚ğ“ü—Í");
  ‡@("%f", &x );
  printf("y‚ğ“ü—Í");
  ‡@("%f", &y );
  printf("z‚ğ“ü—Í");
  ‡@("%f", &z );
  k = ( ‡A )/2.0;
  s = sqrt( k*(k-x)*(k-y)*(k-z) );
  printf("–ÊÏ=%f\n", ‡B );

  return 0;
}